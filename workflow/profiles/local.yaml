schema_version: 1
name: local-lightweight
enabled: true
backend: local
scheduler: none
purpose:
  - manuscript_checks
  - literature_metadata_checks
  - lightweight_postprocessing
run_root: .research-runs
max_parallel_jobs: 1
allow_heavy_compute: false

